# Variables de la stack demo (Compose). Copia a .env si quieres overrides locales.
# Nunca commitees secretos reales.

DATABASE_URL=postgres://lab:lab@postgres:5432/lab
REDIS_URL=redis://redis:6379/0
PORT=8081
SERVICE_NAME=cloudnative-demo-api
LAB_SLOW_SECONDS=3
